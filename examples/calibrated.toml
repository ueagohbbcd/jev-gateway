# Standalone experimental configuration; no inheritance or overlay rules.
[upstream]
base_url = "https://api.deepseek.com"
model = "deepseek-flash"
api_key_env = "DEEPSEEK_API_KEY"
extra_body = { thinking = { type = "disabled" } }

[adapter]
# Approximate optimum for an older A/B experiment, NOT validated for this prompt.
temperature = 2.8
double_round_robin = false

# Omitted prompt/server fields use built-in defaults.
